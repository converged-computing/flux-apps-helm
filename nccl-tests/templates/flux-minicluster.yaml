{{ include "base-template.flux-minicluster" . }}
    environment:	
      apprun: '{{ .Values.nccl-tests.binary }} -b {{ .Values.nccl-tests.b }} -e {{ .Values.nccl-tests.e }} -e {{ .Values.nccl-tests.e }} -f {{ .Values.nccl-tests.f }} -g {{ .Values.nccl-tests.g }}'
      {{- include "base-template.environment" . }}
# flux run -n 2 -N 2 -g 1 ./nccl-tests/build/all_reduce_perf -b 8 -e 8G -f 2 -g 1

