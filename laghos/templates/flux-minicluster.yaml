{{ include "base-template.flux-minicluster" . }}
    environment:
      apprun: '{{ default "laghos" .Values.laghos.binary }} {{ if .Values.laghos.mesh }}--mesh {{ .Values.laghos.mesh }}{{ end }} {{ if .Values.laghos.max_steps }}--max-steps {{ .Values.laghos.max_steps }}{{ end }}'
      {{- include "base-template.environment" . }}
